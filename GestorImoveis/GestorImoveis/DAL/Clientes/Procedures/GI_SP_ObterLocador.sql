CREATE PROCEDURE GI_SP_ObterLocador
 
 @CODCLI varchar(14)

AS

IF(ISNULL(@CODCLI, '') != '')
BEGIN
 SELECT 
 NOMECLI
 FROM CCLIENTES
 WHERE CODCLI = @CODCLI AND TIPOCLI = 'Locador'
END
ELSE
BEGIN
 SELECT 
 NOMECLI
 FROM CCLIENTES
 WHERE TIPOCLI = 'Locador'
END
GO

